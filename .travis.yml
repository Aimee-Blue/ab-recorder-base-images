language: minimal
script:
  - docker build --target=ab-recorder-build-base . -t aimeeblue/ab-recorder-build-base -q
  - docker build --target=ab-recorder-run-base . -t aimeeblue/ab-recorder-run-base -q
deploy:
  provider: script
  script: ./ci-push.sh 
  on:
    branch: master
